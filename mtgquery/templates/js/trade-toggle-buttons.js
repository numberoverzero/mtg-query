$('#cache-toggle-button').toggleButtons({
    width: 250,
    label: {
      enabled: "Use Cached",
      disabled: "Force Lookup"
      },
    style: {
      enabled: "success",
      disabled: "danger"
    }
});